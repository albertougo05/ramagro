<!DOCTYPE html>
 <html lang="en">
   <head>
     <meta charset="utf-8">
     <!--Import materialize.css-->
     <link type="text/css" rel="stylesheet" href="{{ base_url() }}/css/materialize.min.css"  media="screen,projection"/>
     <link type="text/css" rel="stylesheet" href="{{ base_url() }}/css/estilos-login.css"/>
     <link rel="icon" type="image/png" href="{{ base_url() }}/img/ramagro.ico">

     <!--Let browser know website is optimized for mobile-->
     <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
     <title>Ramagro - Login</title>
     <style type="text/css">
       body {
         background: url("{{ base_url() }}/img/imagen-login.jpg");
         background-size: cover;
         background-repeat: no-repeat;
         background-attachment: fixed;
       }
     </style>
   </head>
   <body>
      <noscript>Debe activar javascript para usar esta aplicación.</noscript>
      <div class="ramagroLogo">
          <img src="img/ramagro.png" alt="ramagroLogo">
      </div>

      <div class="tridimLogo">
          <img src="img/tridimLogo.png" alt="tridimLogo" width="100" height="100">
      </div>

      <div class="box">
        <h3>Acceso</h3>
          <form method="post" action="{{ path_for('postlogin') }}">

              <div class="inputBox">
                <div class="input-field col s4">
                  <input type="text" class="validate white-text" name="user" id="user" required>
                  <label for="user">Usuario</label>
                </div>
              </div>

              <div class="inputBox">
                <div class="input-field col s4">
                  <input id="passw" name="passw" type="password" class="validate white-text">
                  <label for="password">Contraseña</label>
                </div>
              </div>
              <div class="center-align">
                <input type="submit" name="btnIngresar" id="btnIngresar" value="Ingresar">
              </div>
                {# Para CSRF control #}
                {{ csrf.field | raw }}
           </form>
       </div>

     <!--JavaScript at end of body for optimized loading-->
     <script type="text/javascript" src="{{ base_url() }}/js/materialize.min.js"></script>
     <script>

      {% if flash.getMessage('error') %}
        M.toast({html: '{{ flash.getMessage('error') | first }}', classes: 'red'});
      {% endif %}

    </script>

   </body>
 </html>
